name: Deploy
on:
  push:
    branches:
      - main
      - dev
  pull_request: {}

permissions:
  actions: write
  contents: read



jobs:

  build: 
    name: build
    uses: ./.github/workflows/build.yml
    secrets: inherit


  #deploy:
    #name: Deploy
    #runs-on: 
    #if: ${{github.ref == 'refs/heads/main' && github.event_name == 'push'}}
